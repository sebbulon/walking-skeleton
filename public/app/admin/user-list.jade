//
   Created by sebastian.weikart on 18/03/2015.

.container
    table.table
        tr(ng-repeat="user in users")
            td {{user.firstName + ' ' + user.lastName}}
            td {{user.userName}}